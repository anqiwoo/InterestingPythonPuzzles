# # Syntax
# numpy.zeros(shape[, dtype=float[, order='C'[, *[, like=None]]]])¶

# Basic Example 1
# shape = (3, 3)
# all_zeros_array = np.zeros(shape)
# print(all_zeros_array)

# Basic Example 2
import numpy as np
shape = 3
all_zeros_array = np.zeros(shape)
print(all_zeros_array)
